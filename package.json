{
  "name": "agape-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/school-frontend-app && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^3.0.1",
    "prisma": "^6.6.0"
  }
}
